# 短闻 — news.drupal.org.cn / duanwen.drupal.org.cn → XMT sites/drupal.org.cn
server
{
    listen 80;
    server_name news.drupal.org.cn duanwen.drupal.org.cn;
    index index.php index.html index.htm;
    root /home/wwwroot/xmt/web;

    location ^~ /.well-known/ {
        allow all;
        default_type text/plain;
    }

    location ~ /\. {
        deny all;
    }

    location / {
        return 301 https://$host$request_uri;
    }

    access_log /home/wwwlogs/news.drupal.org.cn.log;
}

server
{
    listen 443 ssl;
    http2 on;
    server_name news.drupal.org.cn duanwen.drupal.org.cn;
    index index.php index.html index.htm;
    root /home/wwwroot/xmt/web;

    ssl_certificate /usr/local/nginx/conf/ssl/news.drupal.org.cn/fullchain.cer;
    ssl_certificate_key /usr/local/nginx/conf/ssl/news.drupal.org.cn/news.drupal.org.cn.key;
    ssl_session_timeout 5m;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers "EECDH+CHACHA20:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:!MD5";
    ssl_session_cache builtin:1000 shared:SSL:10m;
    ssl_dhparam /usr/local/nginx/conf/ssl/dhparam.pem;

    include proxy-pass-php.conf;

    location ~* \.(gif|jpg|jpeg|png|bmp|swf|ico|woff2?|ttf|eot|svg)$
    {
        expires 30d;
        try_files $uri @apache;
    }

    location ~* \.(js|css)$
    {
        expires 12h;
        try_files $uri @apache;
    }

    location ^~ /.well-known/ {
        allow all;
        default_type text/plain;
    }

    location ~ /\. {
        deny all;
    }

    access_log /home/wwwlogs/news.drupal.org.cn.log;
}
