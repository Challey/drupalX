<?php

/**
 * @file
 * Primary module hooks for DrupalX Portal.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function dcn_portal_help($route_name, RouteMatchInterface $route_match): string {
  switch ($route_name) {
    case 'help.page.dcn_portal':
      return '<p>' . t('Provides portal front pages for products and media center content.') . '</p>';

    default:
      return '';
  }
}

/**
 * Implements hook_theme().
 */
function dcn_portal_theme(array $existing, $type, $theme, $path): array {
  return [
    'dcn_portal_product_list' => [
      'variables' => ['products' => []],
    ],
    'dcn_portal_media_list' => [
      'variables' => ['items' => []],
    ],
  ];
}
