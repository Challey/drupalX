services:
  dx_appstore.commands:
    class: Drupal\dx_appstore\Commands\AppStoreCommands
    arguments:
      - '@entity_type.manager'
      - '@dx_appstore.installer'
    tags:
      - { name: drush.command }

